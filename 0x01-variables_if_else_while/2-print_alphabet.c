#include <stdio.h>
/**
 * main - Entry point
 * Last digits
 * Return: Always 0 (Success)
 */
int main(void)
{
char wow;
wow = 'a';
while (wow <= 'z')
{
putchar(wow);
wow++;
}
putchar('\n');
return (0);
}
