#include "holberton.h"
#include <stdio.h>


/**
 * rev_strin - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
void rev_string(char *s)
{
    char *str;

    str = "Holberton";
    _puts(str);
    

    str = "notrebloH";
    _puts(str);
    }

   
