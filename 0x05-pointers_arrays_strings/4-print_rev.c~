#include "holberton.h"

/**
 *print_rev  - prints a string followed by a new line
 * @str: string.
 * Return: nothing;
 */
void print_rev(char *s);
{
(char *str)
	int i;
	char c;

	for (i = 0; str[i] != 0; i++)
	{
		c = str[i];
		_putchar(c);
	}
	_putchar('\n');
}
