#include <stdio.h>
#include "lists.h"
/**
 * print_listint -  for holbertonschool .
 * @h: var
 * Return: number
 */
size_t print_listint(const listint_t *h)
{
size_t s = 0;
while (h != '\0')
{
printf("%d\n", h->n);
s++;
h = h->next;
}
return (s);
}
