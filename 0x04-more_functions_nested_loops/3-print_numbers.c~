#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
void print_numbers(void)
{
  int a;
  while ((a = 0) && (a < 10))
    {
      _putchar(a + '0');
      a++;
    }
  _putchar('\n');
}
